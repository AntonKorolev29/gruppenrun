from aiogram import types, F, Router
from aiogram.filters import StateFilter
from keyboards.reply import main_kb, admin_kb
from config import ADMIN_ID

router = Router()

# ===== ОБРАБОТЧИК НЕИЗВЕСТНЫХ СООБЩЕНИЙ =====
@router.message(StateFilter(None))
async def unknown_message_handler(message: types.Message):
    """Обработчик для всех необработанных текстовых сообщений вне состояний FSM"""
    user_id = str(message.from_user.id)
    is_admin = user_id == str(ADMIN_ID)
    
    await message.answer(
        "🤖 Я работаю через кнопки меню!\n\n"
        "📌 Чтобы управлять ботом, используй кнопки ниже:\n\n"
        "• ✅ Зарегистрироваться на Группенран\n"
        "• 🗺 Кругосветка 2025\n"
        "• 🏔 Иремель Кэмп 2025\n"
        "• 👤 Мой профиль\n"
        "• 📍 Как добраться\n"
        "• ❓ Вопросы и предложения\n\n"
        "💡 Если кнопки не отображаются — нажми на иконку меню (📋 или ☰) "
        "справа от поля ввода сообщений.",
        reply_markup=admin_kb if is_admin else main_kb
    )
